# 📖 راهنمای جامع استفاده از ربات هوشمند GramCRM
### ویژه ادمین‌ها، فروشگاه‌ها و صاحبان کسب‌وکار اینستاگرام

به **GramCRM** خوش آمدید! این سامانه یک دستیار هوشمند ۲۴ ساعته برای پیج اینستاگرام شماست که بدون قطعی، بدون افت سرعت و بدون هیچ‌گونه ریسک مسدودی یا شادوبن (به دلیل اتصال به API رسمی متا و زرنیو)، تمامی دایرکت‌ها و تعاملات مشتریان شما را مدیریت می‌کند.

این راهنما گام‌به‌گام و به زبان کاملاً ساده نوشته شده تا بتوانید بدون نیاز به هیچ دانش فنی، تمام امکانات ربات خود را راه‌اندازی و مدیریت کنید.

---

## 📑 فهرست مطالب
1. [ورود به پنل مدیریت](#1-ورود-به-پنل-مدیریت)
2. [اتصال پیج اینستاگرام (فقط در ۲ دقیقه)](#2-اتصال-پیج-اینستاگرام-فقط-در-۲-دقیقه)
3. [تعریف کلمات کلیدی و پاسخ خودکار (Keywords)](#3-تعریف-کلمات-کلیدی-و-پاسخ-خودکار-keywords)
4. [تنظیم دروازه فالو / قفل محتوا (Follow Gate)](#4-تنظیم-دروازه-فالو--قفل-محتوا-follow-gate)
5. [کار با دکمه‌های شیشه‌ای (لینک و پیام سریع)](#5-کار-با-دکمههای-شیشهای-لینک-و-پیام-سریع)
6. [صندوق پیام‌ها و ارتباط با مشتریان (Inbox / CRM)](#6-صندوق-پیامها-و-ارتباط-با-مشتریان-inbox--crm)
7. [مدیریت محصولات و فروشگاه (Products)](#7-مدیریت-محصولات-و-فروشگاه-products)
8. [پشتیبان‌گیری (بکاپ) و به‌روزرسانی پنل با یک کلیک](#8-پشتیبانگیری-بکاپ-و-بهروزرسانی-پنل-با-یک-کلیک)
9. [تغییر رمز عبور و نکات امنیتی](#9-تغییر-رمز-عبور-و-نکات-امنیتی)
10. [پرسش‌های متداول و خطایابی](#10-پرسشهای-متداول-و-خطایابی)

---

## ۱. ورود به پنل مدیریت

برای ورود به داشبورد ربات، از هر دستگاهی (کامپیوتر، لپ‌تاپ یا گوشی موبایل) می‌توانید استفاده کنید:

1. مرورگر خود (Chrome، Safari یا Firefox) را باز کنید.
2. آدرس سرور یا دامنه‌ای که به شما تحویل داده شده را همراه با `/panel/` وارد کنید:
   - نمونه: `http://SERVER_IP/panel/` یا `https://crm.yourdomain.com/panel/`
3. نام کاربری و رمز عبور پیش‌فرض خود را وارد کنید:
   - **نام کاربری پیش‌فرض:** `admin`
   - **رمز عبور پیش‌فرض:** `admin123` (حتماً پس از اولین ورود تغییر دهید).
4. روی دکمه **ورود به سیستم** کلیک کنید.

---

## ۲. اتصال پیج اینستاگرام (فقط در ۲ دقیقه)

سیستم GramCRM به صورت هوشمند و خودکار پیج شما را شناسایی می‌کند و نیازی به وارد کردن دستی شناسه‌ها (ID) ندارد!

### مراحل دریافت توکن Zernio و اتصال:
1. وارد سایت [Zernio.com](https://zernio.com) شوید و یک حساب بسازید.
2. پیج اینستاگرام کاری/تجاری (Business یا Creator) خود را طبق راهنمای Zernio به سیستم متصل کنید.
3. از بخش **API Keys** در پنل Zernio، کلید اختصاصی خود را که با `sk_...` شروع می‌شود کپی کنید.
4. در پنل **GramCRM**، از منوی سمت چپ به تب **⚙️ تنظیمات (Settings)** بروید.
5. در بخش **پیکربندی اینستاگرام (Zernio API)**:
   - کلید API کپی شده را در کادر **Zernio API Key** قرار دهید.
   - شناسه اکانت (Account ID) و شناسه پروفایل (Profile ID) در صورت خالی بودن، به شکل هوشمند توسط ربات تکمیل خواهند شد.
6. روی دکمه **💾 ذخیره تنظیمات** کلیک کنید.
7. روی دکمه **⚡ تست زنده اتصال** کلیک کنید:
   - پیامی به رنگ سبز نشان داده می‌شود که اتصال موفقیت‌آمیز بوده و نام کاربری پیج شما تایید می‌گردد.

---

## ۳. تعریف کلمات کلیدی و پاسخ خودکار (Keywords)

این بخش مهم‌ترین بخش ربات شماست؛ جایی که به ربات یاد می‌دهید در صورت دریافت کلمات خاص، چه جوابی ارسال کند.

### چگونه یک کلیدواژه جدید بسازیم؟
1. به تب **🔑 کلمات کلیدی (Keywords)** بروید.
2. روی دکمه **➕ کلمه کلیدی جدید** کلیک کنید.
3. فیلدها را به شکل زیر پر کنید:
   - **کلمه یا عبارت محرک (Keyword):** کلمه‌ای که کاربر در دایرکت می‌فرستد (مثال: `قیمت`، `سفارش`، `تخفیف`، `تستر`).
   - **نوع تطابق (Match Type):**
     - **دقیق (Exact):** فقط وقتی عین کلمه ارسال شود جواب می‌دهد.
     - **شامل (Contains - پیشنهادی):** اگر این کلمه در بین جمله مشتری هم باشد جواب می‌دهد (مثلاً: «سلام *قیمت* این کار چنده؟»).
   - **متن پاسخ دایرکت (Reply Content):** متنی که مشتری دریافت خواهد کرد (امکان استفاده از ایموجی‌ها، توضیحات قیمت، شماره تماس و...).
   - **وضعیت:** فعال ✅.
4. (اختیاری) می‌توانید برای جذابیت بیشتر، **دکمه‌های شیشه‌ای** اضافه کنید (توضیح در بخش ۵).
5. روی دکمه **ذخیره** کلیک کنید. تغییرات بلافاصله روی اینستاگرام اعمال می‌شود!

---

## ۴. تنظیم دروازه فالو / قفل محتوا (Follow Gate)

یکی از قدرتمندترین ابزارهای رشد فالوور، **Follow Gate** است. با فعال‌سازی این قابلیت، اگر فردی که پیج شما را فالو ندارد پیامی بفرستد، ابتدا از او خواسته می‌شود پیج را فالو کند و پس از فالو، پاسخ اصلی یا لینک هدیه/تخفیف برای او ارسال می‌شود.

### نحوه تنظیم:
1. در پنل به تب **⚙️ تنظیمات (Settings)** و بخش **دروازه فالو (Follow Gate)** بروید.
2. گزینه **فعال‌سازی دروازه فالو** را روی حالت روشن (تیک‌دار) قرار دهید.
3. **متن پیام الزام به فالو:** پیامی که کاربر غیرفالوور دریافت می‌کند را بنویسید.
   - *نمونه:* «سلام دوست عزیز! برای دریافت لینک تخفیف و مشاهده قیمت، ابتدا پیج ما رو فالو کن و بعد روی دکمه "فالو کردم ✅" بزن.»
4. **تنظیم دکمه‌های هوشمند:**
   - **دکمه اول (مشاهده پیج):**
     - نوع: `🔗 باز کردن لینک`
     - عنوان: `مشاهده و فالو کردن پیج 👀`
     - لینک: آدرس پیج شما در اینستاگرام (مثال: `https://instagram.com/yourpage`)
   - **دکمه دوم (تأیید فالو بدون تایپ):**
     - نوع: `💬 ارسال پیام در دایرکت`
     - عنوان: `فالو کردم ✅`
     - متن ارسالی: `فالو کردم`
5. دکمه **💾 ذخیره دروازه فالو** را بزنید.

> 💡 **چرا دکمه «ارسال پیام در دایرکت» معجزه می‌کند؟**
> کاربر نیازی به تایپ کردن ندارد! تنها با لمس دکمه «فالو کردم ✅»، پیام ارسال شده، ربات بررسی می‌کند و در صورت فالو بودن پیج، بلافاصله پاسخ اصلی را برای مشتری می‌فرستد.

---

## ۵. کار با دکمه‌های شیشه‌ای (لینک و پیام سریع)

در هر پاسخ خودکار می‌توانید تا **۳ دکمه شیشه‌ای شیک و حرفه‌ای** قرار دهید:

| نوع دکمه | کاربرد | مثال |
| :--- | :--- | :--- |
| **🔗 باز کردن لینک (URL)** | هدایت مشتری به سایت خرید، کانال تلگرام، پیج همکار یا درگاه پرداخت | `خرید اینترنتی 🛒` ➔ `https://myshop.ir/buy` |
| **💬 ارسال پیام در دایرکت (Message)** | ایجاد منوی تعاملی بدون نیاز به تایپ مشتری (Quick Reply) | `مشاهده کاتالوگ 📚` ➔ ارسال پیام «کاتالوگ» |

### مزیت دکمه‌های شیشه‌ای:
- ظاهر فوق‌العاده حرفه‌ای و معتبر (مانند برندهای بزرگ بین‌المللی).
- افزایش چشمگیر نرخ تبدیل و فروش به دلیل راحتی مشتری در کلیک کردن.

---

## ۶. صندوق پیام‌ها و ارتباط با مشتریان (Inbox / CRM)

پنل GramCRM مجهز به یک سیستم گفتگوی زنده است:

- در تب **💬 پیام‌ها (Messages)** لیست تمامی گفتگوهای اخیر را مشاهده می‌کنید.
- **به‌روزرسانی آنی:** بدون نیاز به رفرش صفحه، پیام‌های جدید در همان لحظه با سیستم اختصاصی وب‌سوکت ظاهر می‌شوند.
- **پاسخ دستی به مشتری:** می‌توانید مستقیماً از داخل پنل برای مشتری پاسخ تایپ کنید و بفرستید. پیام فوراً در دایرکت اینستاگرام مشتری تحویل داده می‌شود.
- **اطلاعات مشتری:** نام کاربری اینستاگرام، آخرین تعامل، و وضعیت خرید مشتری را در یک نگاه بررسی کنید.

---

## ۷. مدیریت محصولات و فروشگاه (Products)

اگر فروشگاه فیزیکی یا آنلاین دارید، می‌توانید کاتالوگ محصولات خود را در سیستم تعریف کنید:

1. به تب **🛍️ محصولات (Products)** بروید.
2. روی دکمه **➕ افزودن محصول** کلیک کنید.
3. نام محصول، قیمت، موجودی انبار، و کد کالا (SKU) را وارد و ذخیره کنید.
4. این اطلاعات به شما کمک می‌کند در هر لحظه موجودی انبار و کالاهای پرفروش خود را زیر نظر داشته باشید.

---

## ۸. پشتیبان‌گیری (بکاپ) و به‌روزرسانی پنل با یک کلیک

### ۱. پشتیبان‌گیری (Backup):
- برای اینکه خیالتان از اطلاعات، پیام‌ها و کلمات کلیدی راحت باشد، در تب **⚙️ تنظیمات**:
- روی دکمه **📥 دانلود فایل پشتیبان (JSON)** کلیک کنید.
- فایلی روی کامپیوتر یا موبایل شما دانلود می‌شود که کل دیتابیس ربات شما در آن محفوظ است.
- در صورت نیاز به بازگردانی، کافیست همان فایل را از بخش **بازگردانی پشتیبان** انتخاب کرده و آپلود کنید.

### ۲. آپدیت خودکار سیستم (1-Click Updater):
- هر زمان امکانات جدیدی به ربات اضافه شود یا باگ‌های احتمالی رفع گردند:
- در بخش تنظیمات، روی **🔄 بررسی به‌روزرسانی** کلیک کنید.
- سیستم به گیت‌هاب متصل شده و نسخه جدید را بررسی می‌کند.
- با زدن دکمه **🚀 به‌روزرسانی به آخرین نسخه**، ربات در عرض چند ثانیه بدون پاک شدن اطلاعات شما آپدیت می‌شود.

---

## ۹. تغییر رمز عبور و نکات امنیتی

برای حفظ امنیت کسب‌وکارتان، حتماً پس از تحویل ربات رمز عبور پیش‌فرض را تغییر دهید:

1. به تب **⚙️ تنظیمات** بروید.
2. به بخش **امنیت و حساب کاربری ادمین** مراجعه کنید.
3. نام کاربری و رمز عبور جدید دلخواهتان را وارد کنید (حداقل ۸ کاراکتر و ترکیبی از حروف و اعداد).
4. روی **💾 ذخیره تنظیمات** کلیک کنید.

---

## ۱۰. پرسش‌های متداول و خطایابی

#### س: آیا ممکن است پیج من بلاک یا دیس‌ابل شود؟
**ج:** خیر! ربات‌های سنتی از پسورد پیج و شبیه‌ساز اندروید استفاده می‌کردند که اینستاگرام آن‌ها را شناسایی و بن می‌کرد. اما GramCRM از **زیرساخت رسمی API ابری Meta** استفاده می‌کند و ۱۰۰٪ ایمن و استاندارد است.

#### س: اگر کاربر به جای یک کلمه، یک جمله کامل فرستاد چه می‌شود؟
**ج:** اگر نوع کلمه کلیدی را روی حالت **شامل (Contains)** قرار داده باشید، اگر کلمه کلیدی در هر کجای جمله مشتری باشد، ربات آن را تشخیص داده و پاسخ می‌دهد.

#### س: چگونه متوجه شوم ربات در حال حاضر فعال و آنلاین است؟
**ج:** در بالای داشبورد، وضعیت سیستم به رنگ سبز و با عبارت `Online` نمایش داده می‌شود. همچنین با زدن دکمه «تست زنده اتصال» در تب تنظیمات می‌توانید اتصال زنده را در ۱ ثانیه بسنجید.

---

<div align="center">

**با استفاده از GramCRM، هیچ مشتری و سفارشی را حتی در زمان خواب از دست نخواهید داد! ✨**

</div>
